// PostCSS configuration file
// PostCSS is a tool that processes CSS through JavaScript plugins

export default {
  // Define which PostCSS plugins to use and how to configure them
  plugins: {
    // Autoprefixer plugin: automatically adds vendor prefixes (-webkit-, -moz-, etc.) for browser compatibility
    autoprefixer: {},
  },
}
